# ─────────────────────────────────────────────────────────────
# Socratic Debate Coach Agent — Requirements
# LangChain 1.0 + LangGraph 1.0 | Python 3.10+
# ─────────────────────────────────────────────────────────────

# Core frameworks (v1.0 stable releases — October 2025)
langchain>=1.0.0
langgraph>=1.0.0
langchain-core>=0.3.0

# LLM provider integrations (choose one or more)
langchain-openai>=0.3.0         # OpenAI GPT-4o / GPT-4o-mini
langchain-anthropic>=0.3.0      # Anthropic Claude 3.5 Sonnet
langchain-google-vertexai>=2.0  # Google Gemini 2.0

# LangGraph persistence (built-in checkpointing)
# MemorySaver is included in langgraph core
# For production persistent storage, add:
# langgraph-checkpoint-postgres>=1.0.0

# Observability (optional but recommended)
langsmith>=0.2.0                # LangSmith tracing + evaluation

# Utilities
python-dotenv>=1.0.0            # .env file management
pydantic>=2.7.0                 # Type validation (used by LangChain internally)
typing-extensions>=4.12.0       # TypedDict, Annotated support

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.0
